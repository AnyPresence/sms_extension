.text-info
  %h3 Send SMS
= simple_form_for(@message, :url => send_sms_path, :html => {class: 'form-horizontal'}) do |f|
  .form-inputs
    = f.error :base
    = f.error_notification
    = f.input :from
    = f.input :to
    = f.input :body

  .form-actions
    = f.button :submit, "Send SMS", {class: 'btn btn-primary'}